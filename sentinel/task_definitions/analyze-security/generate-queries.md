You are designing search queries that will surface findings for a 5-10 year investment view of `{{name}}`.

Profile:

{{profile}}

Write 3-5 search queries tailored to this specific security. Day-to-day and quarterly performance are noise; structural trajectory is signal.

Write the queries as valid JSON to this exact file. The file must contain only a JSON array of strings, for example: `["first query","second query"]`

`{{queriesPath}}`
